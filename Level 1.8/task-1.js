// Заполните массив целыми числами от 1 до 10.

function fillArray() {
	let newArray = [];
	for (let index = 1; index <= 10; index++) {
		newArray.push(index);
	}
	console.log(newArray);
}

fillArray();
